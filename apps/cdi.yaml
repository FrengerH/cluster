apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cdi
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
  destination:
    name: in-cluster
    namespace: cdi
  sources:
    - repoURL: 'https://github.com/frengerh/cluster'
      path: apps/cdi-manifests
      targetRevision: HEAD
  project: default
